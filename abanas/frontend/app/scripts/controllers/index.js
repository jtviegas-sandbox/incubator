'use strict';

/**
 * @ngdoc function
 * @name fassApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the fassApp
 */
angular.module('abanas')
  .controller('indexCtrl', function ($scope, $alert) {

/*	$scope.modal = {
		title: 'Modal Title',
		content: 'Modal content'
	};

	$scope.tooltip = {
		title: 'Tooltip Title'
		};
	$scope.popover = {
		title: 'Title',
		content: 'Popover content',
		trigger: 'focus'
	};
	$scope.alert = {
		title: 'Title',
		content: 'Alert content',
		type: 'success'
		};

	var alert = $alert({
              title: 'Alert Title!',
              content: 'Here\'s some content.',
              type: 'danger',
              container: '#alertContainer',
              show: false
            });
	$scope.showAlert = alert.show;*/

  });
